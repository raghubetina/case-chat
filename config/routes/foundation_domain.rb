# The Compiler's App-Schema-derived routes landed here: public CRUD over every
# entity, as the authoring substrate before the real screens existed.
#
# They are gone. Authoring a case is Author::*, playing one is Cases/Threads,
# and the rest of the entities are written by the loop rather than by hand, so
# the generated screens were a second un-designed UI over the same data with
# nothing linking to it. This seam stays so a future Compile has somewhere to
# land, and so the Foundation contract still has a file to point at.
