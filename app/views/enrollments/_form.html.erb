<%= form_with model: record,
  scope: :enrollment,
  url: (record.persisted? ? enrollment_path(record) : enrollments_path),
  method: (record.persisted? ? :patch : :post),
  class: "space-y-6" do |form| %>
  <%= hidden_field_tag :return_to, return_to %>

  <% if record.errors.any? %>
    <section class="alert alert-error" role="alert" aria-labelledby="foundation-form-errors">
      <div>
        <h2 id="foundation-form-errors" class="font-semibold"><%= t("foundation_domain.form.errors.heading") %></h2>
        <ul class="mt-2 list-disc pl-5">
          <% record.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    </section>
  <% end %>

  <% if record.persisted? %>
    <div>
      <%= form.label :user_id, I18n.t("foundation_domain.entities.enrollments.fields.user_id"), class: "label font-medium" %>
      <%= form.select :user_id, @user_id_options, {include_blank: true}, {class: "select select-bordered w-full", required: true} %>
      <% record.errors.full_messages_for(:user).each do |message| %>
        <p class="mt-1 text-sm text-error"><%= message %></p>
      <% end %>
    </div>
    <div>
      <%= form.label :case_study_id, I18n.t("foundation_domain.entities.enrollments.fields.case_study_id"), class: "label font-medium" %>
      <%= form.select :case_study_id, @case_study_id_options, {include_blank: true}, {class: "select select-bordered w-full", required: true} %>
      <% record.errors.full_messages_for(:case_study).each do |message| %>
        <p class="mt-1 text-sm text-error"><%= message %></p>
      <% end %>
    </div>
  <% else %>
    <div>
      <%= form.label :user_id, I18n.t("foundation_domain.entities.enrollments.fields.user_id"), class: "label font-medium" %>
      <%= form.select :user_id, @user_id_options, {include_blank: true}, {class: "select select-bordered w-full", required: true} %>
      <% record.errors.full_messages_for(:user).each do |message| %>
        <p class="mt-1 text-sm text-error"><%= message %></p>
      <% end %>
    </div>
    <div>
      <%= form.label :case_study_id, I18n.t("foundation_domain.entities.enrollments.fields.case_study_id"), class: "label font-medium" %>
      <%= form.select :case_study_id, @case_study_id_options, {include_blank: true}, {class: "select select-bordered w-full", required: true} %>
      <% record.errors.full_messages_for(:case_study).each do |message| %>
        <p class="mt-1 text-sm text-error"><%= message %></p>
      <% end %>
    </div>
  <% end %>

  <div class="flex items-center gap-3">
    <%= form.submit class: "btn btn-primary" %>
    <%= link_to t("foundation_domain.form.cancel"), cancel_to, class: "btn btn-ghost" %>
  </div>
<% end %>
