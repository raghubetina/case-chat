<div id="<%= dom_id(question, :test_pending) %>" class="flex gap-3">
  <div class="flex min-w-0 grow flex-col gap-2">
    <p class="max-w-prose text-base text-mute"><%= t("author.test_drive.failed") %></p>
  </div>
</div>
