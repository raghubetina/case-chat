<nav aria-label="<%= t("foundation_domain.navigation.label") %>">
  <ul class="menu menu-horizontal gap-1 px-1">
    <li>
      <%= link_to t("nav.my_cases"), cases_path, class: "btn btn-ghost btn-sm",
            aria: {current: ("page" if controller_name == "cases" && controller_path == "cases")} %>
    </li>
    <li>
      <%= link_to t("nav.authoring"), author_cases_path, class: "btn btn-ghost btn-sm",
            aria: {current: ("page" if controller_path.start_with?("author/"))} %>
    </li>
  </ul>
</nav>
