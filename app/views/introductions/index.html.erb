<% content_for :title, t("foundation_domain.entities.introductions.index.title") %>

<section class="py-10 sm:py-14">
  <header class="mb-8 flex items-end justify-between gap-4">
    <div>
      <p class="text-sm font-medium text-primary"><%= t("app_name") %></p>
      <h1 class="mt-1 text-2xl font-bold tracking-tight sm:text-3xl"><%= t("foundation_domain.entities.introductions.index.heading") %></h1>
      <p class="mt-2 text-sm/6 text-base-content/70"><%= t("foundation_domain.entities.introductions.index.lede") %></p>
    </div>
    <%= link_to t("foundation_domain.entities.introductions.new.action"), new_introduction_path(return_to: "foundation:mutation-record:show"),
      class: "btn btn-primary" %>
  </header>

  <% if @introductions.load.any? %>
    <div class="overflow-x-auto rounded-box border border-base-300 bg-base-100">
      <table class="table">
        <caption class="sr-only"><%= t("foundation_domain.entities.introductions.index.caption") %></caption>
        <thead>
          <tr>
            <th scope="col"><%= t("foundation_domain.entities.introductions.index.descriptor_label") %></th>
            <th scope="col"><%= t("foundation_domain.entities.introductions.index.actions") %></th>
          </tr>
        </thead>
        <tbody>
          <% @introductions.each do |introduction| %>
            <tr>
              <td>
                <%= link_to introduction_path(introduction), class: "link link-hover font-medium" do %>
                  <%= introduction.created_at.presence || t("foundation_domain.entities.introductions.show.unnamed", id: introduction.id) %>
                <% end %>
              </td>
          <td class="text-right">
            <%= link_to t("foundation_domain.entities.introductions.edit.action"), edit_introduction_path(introduction, return_to: introduction_path(introduction)),
              class: "btn btn-ghost btn-sm" %>
          </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <%= render "shared/foundation_pagination", pagy: @pagy %>
  <% elsif @pagy.count.positive? %>
    <section class="rounded-box border border-dashed border-base-300 bg-base-100 px-6 py-12 text-center">
      <h2 class="text-lg font-semibold"><%= t("foundation_domain.pagination.empty.heading") %></h2>
      <p class="mt-2 text-sm/6 text-base-content/70"><%= t("foundation_domain.pagination.empty.body") %></p>
      <%= link_to t("foundation_domain.pagination.empty.action"), introductions_path,
        class: "btn btn-primary mt-5" %>
    </section>
    <%= render "shared/foundation_pagination", pagy: @pagy %>
  <% else %>
    <section class="rounded-box border border-dashed border-base-300 bg-base-100 px-6 py-12 text-center">
      <h2 class="text-lg font-semibold"><%= t("foundation_domain.entities.introductions.empty.heading") %></h2>
      <p class="mt-2 text-sm/6 text-base-content/70"><%= t("foundation_domain.entities.introductions.empty.body") %></p>
    </section>
  <% end %>
</section>
