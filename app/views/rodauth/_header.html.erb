<% content_for :title, @page_title %>

<header class="mb-7">
  <div class="mb-5 grid size-10 place-items-center rounded-box bg-primary text-sm font-bold text-primary-content" aria-hidden="true"><%= t("app_name").first %></div>
  <h1 class="text-2xl font-bold tracking-tight"><%= @page_title %></h1>
  <p class="mt-2 text-sm/6 text-base-content/70"><%= lede %></p>
</header>
