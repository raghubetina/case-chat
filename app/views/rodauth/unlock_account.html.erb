<%= render "panel" do %>
  <%= render "header", lede: t("auth.unlock_account.lede") %>
  <%= form_with url: rodauth.unlock_account_path, method: :post, data: {turbo: false} do |form| %>
    <%= form.submit rodauth.unlock_account_button, class: "btn btn-primary w-full" %>
  <% end %>
<% end %>
