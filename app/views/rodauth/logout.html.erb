<%= render "panel" do %>
  <%= render "header", lede: t("auth.logout.lede") %>
  <%= form_with url: rodauth.logout_path, method: :post, data: {turbo: false} do |form| %>
    <%= form.submit rodauth.logout_button, class: "btn btn-primary w-full" %>
  <% end %>
<% end %>
